import React from "react";

const Modal = () => {
    return (
        <div className="fixed inset-0 bg-color-gray-500 bg-opacity-50 backdrop-blur-sm">
            Save Paste
        </div>
    )
}

export default Modal;